import AvatarInterface from "@/components/avatar/AvatarInterface";

export default function AvatarPage(): JSX.Element {
    return (
        <div className="h-full">
            <AvatarInterface />
        </div>
    );
}
