import { ContentLayout } from "@cloudscape-design/components";
import Layout from "../common/components/Layout";

const Home = () => {
    return <Layout content={<ContentLayout></ContentLayout>} />;
};

export default Home;
