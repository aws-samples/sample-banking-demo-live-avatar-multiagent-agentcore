.DS_STORE
.vscode

# Word/Office lock files
~$*

.venv
node_modules

__pycache__
dist
.cdk.staging
cdk.out

*.env
.env*

# Generated architecture artifacts at repo root
/architecture.*
/architecture-diagram.*
/export.zip

# CDK context cache. Regenerated on synth and keyed by AWS account id, so
# committing it leaks the deploying account into the repo for no benefit —
# this app only caches availability-zone lookups.
cdk.context.json

# Gartner-RESTRICTED reference material — never commit.
# This branch is the external customer build, so these stay local.
docs/Welcome Packet for Magic Quadrant.pdf
docs/Magic-Quadrant-and-Critical-Capabilities.xlsx
docs/Vendor Demonstration Guidelines*.pdf

# Local experiment sandbox (TalkingHead avatar spike) — never committed.
.sandbox/
