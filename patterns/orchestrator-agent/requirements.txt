# a2a extra: the CALLER side of the A2A hops (fraud consult + section-researcher
# fan-out) imports a2a.client/httpx via utils/a2a_client.py — without it the hop
# dies at discovery with "No module named 'a2a'".
strands-agents[a2a,sagemaker]>=1.33.0,<2.0.0
# >=1.8.0 for BedrockAgentCoreContext.get_config_bundle() — the config-bundle
# system-prompt routing used by AgentCore A/B tests (verified: 1.22.0 has it).
bedrock-agentcore[strands-agents]>=1.8.0,<2.0.0
mcp>=1.26.0,<2.0.0
PyJWT[crypto]>=2.10.1
requests>=2.28.0
# bedrock:CreateEvaluationJob with customMetricConfig (the managed catalog A/B,
# features.bedrock_managed_eval) needs a recent botocore model — pin boto3 to
# the version whose shape was verified end-to-end against us-east-1.
boto3>=1.43.74
opentelemetry-distro
opentelemetry-exporter-otlp
playwright>=1.49.0,<2.0.0
