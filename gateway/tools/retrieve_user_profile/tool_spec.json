[
    {
        "name": "retrieve_user_profile",
        "description": "Retrieve the current user's profile data from DynamoDB. Returns their full profile including preferences and account details.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "user_id": {
                    "type": "string",
                    "description": "Authenticated user identifier (injected by runtime)"
                }
            },
            "required": []
        }
    }
]
