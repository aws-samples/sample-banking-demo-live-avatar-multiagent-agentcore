[
    {
        "name": "nova_canvas_history",
        "description": "Retrieve the image generation history for a session. Returns metadata and fresh presigned URLs for all images generated or edited during the session.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "session_id": {
                    "type": "string",
                    "description": "The session identifier to retrieve image history for"
                },
                "limit": {
                    "type": "integer",
                    "description": "Maximum number of images to return (optional, default = 10)"
                }
            },
            "required": ["session_id"]
        }
    }
]
