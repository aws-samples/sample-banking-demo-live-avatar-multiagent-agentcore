[
    {
        "name": "nova_canvas_history",
        "description": "Retrieve the images this user has generated or edited. Returns metadata and fresh presigned URLs, newest first. The caller is identified by the runtime, so no identifier is needed.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "limit": {
                    "type": "integer",
                    "description": "Maximum number of images to return (optional, default = 10)"
                }
            }
        }
    }
]
