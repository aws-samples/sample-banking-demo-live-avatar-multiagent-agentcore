[
    {
        "name": "analyze_patterns",
        "description": "Summarize this user's conversation history from AgentCore Memory. Returns short-term activity (session count, number of exchanges, first and last activity, recent turns) and long-term extraction (record count grouped by memory strategy, with recent extracted memories). Use for questions about how much has been discussed or what has been remembered over time; use recall_memories to look up a specific fact.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "user_id": {
                    "type": "string",
                    "description": "Authenticated user identifier (injected by runtime)"
                }
            },
            "required": []
        }
    }
]
